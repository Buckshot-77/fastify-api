API creation experiment using fastify and the new eslint config format. Just for exercising purposes, but thought it would be a good idea to leave it public.

This course follows the Rocketseat Node.js trail, which is available at https://www.rocketseat.com.br.

The project uses pnpm as the package manager.